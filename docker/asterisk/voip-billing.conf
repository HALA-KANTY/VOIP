# Lu par les scripts AGI (asterisk-integration/agi-bin/*.py). Le secret doit
# correspondre a AMI_ENDPOINTS_SECRET dans le .env a la racine du projet.
# "backend" resout via le reseau Docker interne (service docker-compose).

api_base = http://backend:8000
ami_endpoints_secret = dev_secret_endpoints
timeout = 3
