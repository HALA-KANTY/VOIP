# A copier vers /etc/asterisk/voip-billing.conf (lecture seule pour root,
# car il contient un secret). Lu par les scripts AGI agi-bin/*.py.

api_base = http://192.168.1.101
ami_endpoints_secret = savant_fou2.0
timeout = 3
